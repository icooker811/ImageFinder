# ImageFinder

search locations by using image.
===============================
Config
- install tomcat
- install mysql
  - import mysql "photo.sql"
  - create map, polygon 
  - import images into server
